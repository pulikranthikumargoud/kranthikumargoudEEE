# Telegram bot with webhook support
python-telegram-bot[webhooks]==21.10

# For making API requests (OpenRouter, etc.)
requests==2.32.3
httpx==0.28.1

# Optional: only if you want a small Flask endpoint for health check
Flask==3.0.3
